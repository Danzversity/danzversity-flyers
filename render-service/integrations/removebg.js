// Remove.bg API wrapper — Claude Code will build this
// Endpoint: POST https://api.remove.bg/v1.0/removebg
// Auth: X-Api-Key header
// Input: image_file (multipart), size: 'auto'
// Output: PNG with transparent background
module.exports = { removeBackground: async (imageBuffer) => { throw new Error('Not yet implemented'); } };
