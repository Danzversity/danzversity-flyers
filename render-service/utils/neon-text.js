// Neon text rendering utilities — Claude Code will build this
// Two modes:
//   1. Solid neon: white fill + multi-layer colored glow (for titles)
//   2. Outline neon: transparent fill + colored stroke + glow (for subtitles)
// Both use 3-pass gaussian blur: 30px (×2), 12px, 4px
module.exports = {
  drawSolidNeon: async (sharpInstance, text, options) => { throw new Error('Not yet implemented'); },
  drawOutlineNeon: async (sharpInstance, text, options) => { throw new Error('Not yet implemented'); },
};
